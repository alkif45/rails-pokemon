<div class="container">

  <h1 class="text-white mt-3">Pokedex</h1>

  <h5><%= link_to "Trainers", trainers_path, class: "underlined golden" %></h5>
  <h5><%= link_to "Random Pokémon", trainers_path, class: "underlined golden" %></h5>

  <div class="row mt-3">
    <% @pokemons.each do |pokemon| %>
      <div class="col-md-3 mb-3">
        <div class="card bg-light-blue">
          <%= image_tag url_for(pokemon.photo), alt: "Pokemon picture", class: "pokemon-pic-rounded" %>
          <h4 class="text-white text-center"><%= pokemon.name %></h4>
          <h5 class="golden text-center"><%= pokemon.element_type %></h5>
        </div>
      </div>
    <% end %>
  </div>
</div>
